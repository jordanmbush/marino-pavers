# Build output
dist/
.astro/

# Infrastructure (SST's vendored platform + state)
.sst/
sst-env.d.ts

# Dependencies
node_modules/

# Test artifacts
coverage/

# Local env — the Cloudflare token and dev-stage URLs live here, never in git
.env
.env.local
.env.*.local

# Editor / OS
.DS_Store
*.tsbuildinfo
.eslintcache
.vscode/settings.json
