# Build output
dist/
.astro/

# Infrastructure (SST's vendored platform + state)
.sst/
sst-env.d.ts

# Dependencies
node_modules/

# Test artifacts
coverage/

# Local env — the Cloudflare token (.env.production) and dev-stage URLs
# (apps/web/.env) live here, never in git. Only the examples are tracked.
.env
.env.*
!.env.example

# Editor / OS
.DS_Store
*.tsbuildinfo
.eslintcache
.vscode/settings.json
