---
import Icon from "@/components/ui/Icon.astro";
import Reveal from "@/components/ui/Reveal.astro";
import SectionHeading from "@/components/ui/SectionHeading.astro";
import { getProcess } from "@/services/content";
import { i18n } from "@/services/i18n";

const { locale, t } = i18n(Astro.currentLocale);
const steps = await getProcess(locale);
---

<section class="bg-taupe-50">
  <div class="shell py-20 lg:py-28">
    <SectionHeading {...t.home.process} />

    <ol class="mt-16 grid gap-10 sm:grid-cols-2 lg:grid-cols-5">
      {
        steps.map((step, i) => (
          <Reveal tag="li" delay={i * 90} class="relative flex flex-col gap-4">
            <div class="flex items-center gap-3">
              <span class="font-display text-4xl font-medium text-taupe-500">
                {String(step.order).padStart(2, "0")}
              </span>
              <span class="h-px flex-1 bg-taupe-900/15" />
              <Icon name={step.icon} class="h-5 w-5 text-taupe-900/50" />
            </div>
            <h3 class="text-2xl leading-tight">{step.title}</h3>
            <p class="text-sm leading-relaxed text-taupe-900/65">{step.body}</p>
          </Reveal>
        ))
      }
    </ol>
  </div>
</section>
