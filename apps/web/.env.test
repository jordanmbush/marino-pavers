# Loaded by Vitest over `.env`. Tests run against no stage, the way CI does:
# the admin API on the page's own origin, media root-relative.
PUBLIC_API_URL=
PUBLIC_MEDIA_URL=
PUBLIC_COGNITO_CLIENT_ID=test-client
PUBLIC_AWS_REGION=us-west-1
